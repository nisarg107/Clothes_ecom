import 'package:flutter/material.dart';

class OrderFragScreen extends StatelessWidget {
  const OrderFragScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}